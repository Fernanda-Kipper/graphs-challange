import { DISTANCES, VERTICES } from '../constants/distances'

export const calculateDistance = (origin, destination) => {
  // TODO: faça seu algortimo aqui, faça a função da forma que achar melhor, inclusive os parâmetros
}